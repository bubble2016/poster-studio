# 项目速览

本文档用于新对话或新人接手时快速理解 `D:\海报生成器` 的架构。项目是一个 Flask + 原生前端的公告海报生成工具，面向打包站价格公告、放假通知、调价说明等场景。

## 一句话架构

浏览器页面通过 `static/app.js` 收集表单、价格表和素材配置，请求 Flask 后端 `app.py`；后端调用 `poster_engine.py` 用 Pillow 生成 1080x1920 海报图片，再把预览图、导出文件、用户配置和上传素材存到 `web_data/`。

## 启动方式

- 一键启动：双击 `一键启动.bat`
- 手动启动：

```powershell
py -m pip install -r requirements.txt
py app.py
```

- 本地访问：`http://127.0.0.1:5173`
- 服务监听：`app.py` 末尾 `app.run(host="0.0.0.0", port=5173, ...)`

## 主要目录

```text
D:\海报生成器
├─ app.py                 # Flask 入口、API、用户/文件/配置管理
├─ poster_engine.py       # Pillow 海报渲染核心、模板、默认配置、价格文本处理
├─ static/
│  ├─ app.js              # 前端主逻辑：状态、表单、预览、导出、登录、拖拽、上传
│  ├─ style.css           # 全站样式、响应式、价格表、弹窗、预览区
│  └─ admin.js            # 管理后台前端逻辑
├─ templates/
│  ├─ index.html          # 主页面
│  ├─ admin.html          # 管理后台页面
│  ├─ terms.html          # 用户协议
│  └─ privacy.html        # 隐私政策
├─ presets/
│  ├─ backgrounds/        # 预设背景图
│  └─ logos/              # 默认 Logo
├─ fonts/                 # 中文字体和数字字体
├─ web_data/              # 运行时数据，部署时要持久化，不要随代码覆盖
└─ requirements.txt       # Flask / Pillow / pytest / redis
```

## 核心文件职责

### `app.py`

后端总入口，主要职责：

- 初始化 Flask、Session、安全 Cookie、上传限制、日志和运行目录。
- 管理运行时数据目录：`web_data/uploads`、`web_data/outputs`、`web_data/user_configs`、`web_data/users.json`、`web_data/output_index.json`。
- 登录/注册：简单用户 ID + 密码，密码哈希存在 `users.json`；未登录时自动创建 `guest_xxxxx` 访客身份。
- 配置读写：每个用户一个 JSON 配置文件，路径在 `web_data/user_configs/<user>.json`。
- 素材上传：限制图片类型和体积，保存到 `web_data/uploads`。
- 调用 `poster_engine.draw_poster()` 生成预览和最终图片。
- 管理预览缓存：优先 Redis，Redis 不可用时走本地内存缓存。
- 管理后台：用户列表、导出、备份、改配置、删用户、清理访客。

主要 API：

```text
GET  /                         主页面
GET  /admin                    管理后台
GET  /api/me                   当前身份
POST /api/login                登录或注册
POST /api/logout               退出并回到访客
GET  /api/init                 初始配置、系统模板、预设背景、默认 Logo
POST /api/upload               上传背景/Logo/印章/二维码
POST /api/preview              生成预览，返回 base64 和缓存 URL
GET  /api/preview-image/<id>   读取预览缓存图
POST /api/generate             导出 PNG/JPEG/PDF
GET  /download/<path>          下载用户自己的导出文件
GET  /asset/<path>             读取允许的上传图或预设图
POST /api/config               保存用户配置
POST /api/format               自动格式化正文
POST /api/validate             校验正文
POST /api/batch-adjust         批量调价
```

### `poster_engine.py`

海报生成核心，主要职责：

- 定义画布尺寸：`CANVAS_SIZE = (1080, 1920)`。
- 定义系统模板：`SYSTEM_TEMPLATES`，如 `报价模板`、`调价模板`、`放假模板`、`文字模版`。
- 定义模板元信息：`SYSTEM_TEMPLATE_META`，控制是否价格编辑器、是否文字通知布局、是否自动使用节日红背景。
- 定义默认配置：`DEFAULT_CONFIG`，包括店铺信息、主题色、背景、Logo、印章、二维码、卡片样式、水印、导出格式等。
- 字体选择：优先使用项目内 `fonts/`，再回退 Windows 系统字体。
- 价格文本处理：`auto_format_content()`、`validate_content()`、`batch_adjust_content()`。
- 海报绘制：`draw_poster(content, date_str, title, cfg)`，内部处理背景、模糊、亮度、卡片样式、文本布局、价格高亮、Logo/印章/二维码、水印。
- 配置读写：`load_config()`、`save_config()`。

常见改动位置：

- 新增系统模板：改 `SYSTEM_TEMPLATES` 和 `SYSTEM_TEMPLATE_META`。
- 新增默认配置字段：改 `DEFAULT_CONFIG`，并同步 `app.py` 的配置清洗、`static/app.js` 的表单绑定。
- 改海报视觉：优先看 `draw_poster()` 及其附近的布局/绘制辅助函数。
- 改价格识别或调价：看价格相关正则和 `batch_adjust_content()`。

### `templates/index.html`

主页面结构，包含：

- 顶部品牌栏和设置菜单。
- 实时预览区 `#previewStage`、`#previewImage`。
- 编辑区：标题、日期、快捷日期、自动格式化、批量调价。
- 价格表编辑器：`#priceTableBody`，由 `static/app.js` 动态渲染。
- 模板管理：系统模板、自定义模板、导出格式。
- 设置弹窗：店铺信息、主题、背景、素材、水印等。
- 登录弹窗、通用对话框、Logo 裁剪弹窗、移动端价格行抽屉。
- 引入 `SortableJS` 用于价格表拖拽排序。

### `static/app.js`

前端主逻辑，基本是单文件应用。重要区域：

- 顶部 `state`：全局状态，保存配置、模板、用户、价格行、预览序号等。
- API 封装：`api()`。
- 访客草稿：localStorage key 为 `poster_guest_draft_v1`，有效期 30 天。
- 表单配置：`formConfig()`、`bindFromConfig()`、`buildConfigPayloadForSave()`。
- 模板逻辑：`applyTemplateByName()`、`syncEditorModeByTemplate()`。
- 价格编辑器：
  - 结构数据：`state.priceEditorRows`
  - 文本转表格：`parseContentToPriceEditor()`
  - 表格转文本：`buildContentFromPriceEditor()`
  - 渲染表格：`renderPriceEditorTable()`
  - 桌面端直接编辑，移动端打开抽屉编辑。
  - 拖拽排序在 `init()` 中 `Sortable.create(priceTableBody, ...)`。
- 预览：`refreshPreview()` 调 `/api/preview`，并处理慢预览提示、缓存图、base64 图。
- 上传：`uploadFile()`、`uploadBlob()`、Logo 裁剪相关函数。
- 导出：生成按钮调用 `/api/generate`，再下载或在微信浏览器内预览。

### `static/style.css`

全站样式。常见定位：

- 顶栏、卡片、表单基础样式在前半部分。
- 价格编辑器桌面样式在 `.price-editor-table` 附近。
- 拖拽样式在 `.sortable-ghost`、`.sortable-chosen`、`.sortable-drag` 附近。
- 移动端价格表卡片样式在 `@media` 内 `.price-editor-table tbody tr.price-row-summary` 附近。
- 设置弹窗、登录弹窗、抽屉、预览聚焦等样式分散在后半部分。

## 关键数据流

### 页面初始化

1. 浏览器打开 `/`。
2. `templates/index.html` 先尝试从访客草稿读取主题色，减少首屏闪烁。
3. `static/app.js` 的 `init()` 调 `/api/init`。
4. 后端根据当前 session 获取用户或创建访客用户。
5. 返回用户配置、系统模板、模板元信息、预设背景、默认 Logo。
6. 前端把配置绑定到表单，渲染价格表和素材缩略图，触发首次预览。

### 实时预览

1. 用户改标题、日期、价格、设置或素材。
2. 前端把结构化价格表转成隐藏正文 `#contentInput`。
3. `refreshPreview()` 调 `/api/preview`。
4. 后端合并用户配置和请求配置，清洗字段和路径。
5. 后端用内容、日期、标题、配置生成 cache id。
6. 命中缓存则直接返回；未命中则调用 `draw_poster()` 生成 PNG。
7. 前端显示 `image_data` 或 `/api/preview-image/<id>`。

### 最终导出

1. 点击生成按钮。
2. 前端调 `/api/generate`，传正文、标题、日期、配置、导出格式。
3. 后端调用 `draw_poster()`，保存到 `web_data/outputs/<user>/`。
4. 后端记录 `output_index.json`，并按用户保留数量清理旧文件。
5. 前端触发下载，并提供复制文案。

### 用户和数据持久化

- 访客：session 中是 `guest_xxxxx`，浏览器 localStorage 还保存访客草稿。
- 登录/注册：同一个 `/api/login`，不存在用户则创建，存在则校验密码。
- 配置：每个用户一份 `web_data/user_configs/<user_id>.json`。
- 上传：全局存在 `web_data/uploads/`，配置里保存相对路径。
- 导出：按用户存在 `web_data/outputs/<user_id>/`，下载时校验归属。

## 环境变量

常用：

```text
POSTER_DATA_DIR                     运行时数据目录，部署建议放代码目录外
POSTER_APP_SECRET                   Flask session 密钥，生产必须设置
POSTER_ENV / FLASK_ENV              prod/production 时启用生产校验
POSTER_DEV_RELOAD                   1/true 时开启 Flask 自动重载
POSTER_SESSION_COOKIE_SECURE        是否要求 HTTPS Cookie
POSTER_MAX_SAVED_OUTPUTS_PER_USER   每用户保留导出文件数量，默认 3
POSTER_PREVIEW_CACHE_TTL            预览缓存秒数，默认 300
POSTER_PREVIEW_CACHE_LOCAL_MAX      本地预览缓存条数
POSTER_ADMIN_TOKEN                  管理后台 API token
REDIS_URL                           如代码环境提供 Redis，可用于预览缓存
```

## 部署注意

- `web_data/` 是运行时数据，发布代码时不要覆盖或删除。
- 支持用 `POSTER_DATA_DIR` 把数据目录迁移到代码目录外。
- 生产环境必须设置 `POSTER_APP_SECRET`。
- 上传限制：15MB；允许 `.png`、`.jpg`、`.jpeg`、`.webp`。
- 项目里有 `redis` 依赖，但 Redis 不可用时会退回本地内存缓存。

## 常见改动指南

- 改页面结构：`templates/index.html`
- 改交互：`static/app.js`
- 改视觉样式：`static/style.css`
- 改海报渲染效果：`poster_engine.py`
- 改 API、安全、数据路径：`app.py`
- 改默认模板：`poster_engine.py` 的 `SYSTEM_TEMPLATES`
- 改模板类型或节日背景自动切换：`SYSTEM_TEMPLATE_META`
- 改默认店铺信息/主题/水印/导出配置：`DEFAULT_CONFIG`
- 改一键启动：`一键启动.bat`
- 改后台：`templates/admin.html`、`static/admin.js`、`app.py` 的 `/api/admin/*`

## 当前测试情况

- `requirements.txt` 包含 `pytest`，但当前仓库没有发现测试目录或测试文件。
- 改动后建议至少手动跑：

```powershell
py app.py
```

然后检查：

- `/` 能打开。
- 改价格表后预览会刷新。
- 上传素材能显示缩略图。
- 生成 PNG/JPEG/PDF 能下载。
- 登录后配置能保存。

## 给新对话的快速提示

如果以后开新对话，可以直接说：

```text
请先阅读 D:\海报生成器\项目速览.md，按里面的架构继续处理这个项目。
```

